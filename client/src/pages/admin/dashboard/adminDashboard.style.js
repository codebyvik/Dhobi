import styled from "styled-components";

export const StyledDashboardHome = styled.div`
  width: 100%;
  height: 200px;
  padding: 1rem 2rem;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(450px, 1fr));
  grid-gap: 1rem;
`;
